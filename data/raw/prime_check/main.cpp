#include <bits/stdc++.h>
#include "libs/utils.cpp"

using namespace std;

int main(){
    int n;
    cin >> n;
    cout << is_prime(n);

    cout << "\nPress any key to exit the program!";
    cin.ignore();
    cin.get();

    return 0;
}
